DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/rag
GEMINI_API_KEY=your-google-gemini-key
# Optional. When set, every endpoint except /health requires this value in the
# X-API-Key request header. Leave unset only for trusted local/dev use.
API_KEY=
